function view(dispatch, model) {
    const main = document.createElement('div')
    const heading = document.createElement('h1')
    main.setAttribute("class", "mw6 center")
    heading.setAttribute("class", "f2 pv 2bb")
    heading.textContent = 'Calorie Counter'

    
}